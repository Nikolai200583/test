/* eslint-disable react/jsx-pascal-case */

import * as Styled from './styles';

const Logo = () => {
  return (
    <Styled.logoBox>
      <Styled.logo src="../../static/logo_welbex.svg"></Styled.logo>;
    </Styled.logoBox>
  );
};

export default Logo;
